{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window capability. Narrowed from core:default to only the namespaces this app actually touches: event (listen for terminal-output / terminal-done and webview onDragDropEvent) and webview (getCurrentWebview()). All other backend functionality is reached via custom tauri::commands, which don't require core permissions.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "core:webview:default",
    "core:window:allow-set-size",
    "core:window:allow-center",
    "opener:default",
    "dialog:default"
  ]
}
